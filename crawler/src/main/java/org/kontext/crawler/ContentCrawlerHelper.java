package org.kontext.crawler;

import org.kontext.crawler.wiki.WikiPersistenceStats;

public class ContentCrawlerHelper {

	/**
	 * Based on the source, the stats shall be persisted.
	 * These stats should be aggregatable at another time offline
	 * by specific runners.
	 * 
	 * @param persistenceStats
	 * @param wiki
	 */
	public static void persistStats(WikiPersistenceStats persistenceStats, CrawlSource wiki) {
		
	}
}
